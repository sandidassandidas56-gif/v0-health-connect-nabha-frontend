{
  "name": "v0-health-connect-nabha-frontend-root",
  "private": true,
  "version": "1.0.0",
  "engines": {
    "node": ">=18"
  },
  "workspaces": [
    "frontend"
  ],
  "scripts": {
    "vercel-build": "npm --prefix frontend ci && npm --prefix frontend run build",
    "build": "npm run vercel-build"
  },
  "dependencies": {
    "dotenv": "^17.2.2",
    "mongoose": "^8.18.1"
  },
  "devDependencies": {
    "@types/node": "^24.5.2"
  }
}
